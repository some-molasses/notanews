import { BODY_FONT } from "@/app/styles";
import { ToastContentProps } from "react-toastify";

// export const Toast: React.FC<ToastContentProps> = ({ closeToast, data }) => {
//   return <div className="toast">{}</div>;
// };
